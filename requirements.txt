# Core
Django==5.1.4
djangorestframework==3.15.2
psycopg[binary]==3.2.3
python-dotenv==1.0.1

# Async / queue
celery==5.4.0
redis==5.2.1

# AI
openai==1.59.6
groq==0.13.1

# Шифрование полей с данными пациентов (медданные шифруем на уровне поля).
# django-fernet-fields-v2 — поддерживаемый форк, шифрует значение перед записью
# в БД (Fernet/AES). Ключ — settings.FERNET_KEYS (из env FIELD_ENCRYPTION_KEY).
django-fernet-fields-v2==0.7
cryptography==48.0.0

# Server
gunicorn==23.0.0

# Dev / testing
requests==2.32.3
